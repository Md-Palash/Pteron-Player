package com.pteron.player

import android.app.Application

/**
 * Stage 1: no initialization needed yet. Kept as an extension point
 * for later stages (e.g. dependency injection, WorkManager setup,
 * MediaStore observers).
 */
class PteronApplication : Application()
