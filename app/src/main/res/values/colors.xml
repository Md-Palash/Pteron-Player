<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only shown for the split second before the app draws its first frame; matches the
         default theme (Terracotta). MainActivity repaints the window with the last used theme
         as soon as it starts. -->
    <color name="splash_background">#FDF8F3</color>
</resources>
